#pragma once
#include <iostream>
#include <stdint.h>
#include <algorithm>
#include <cctype>

#include <windows.h>
#include <Psapi.h>

namespace utils
{
	uint8_t* search(const char* mod_name, const char* pattern, uint32_t len = -1, const char skip = '?')
	{
		uint8_t* ret = nullptr;

		return ret;
	}
}
